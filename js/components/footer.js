/* Footer mount */
export function mount() {
  const footer = document.querySelector('.footer');
  if (!footer) return;
  // Footer is static HTML; this module exists for future dynamic content
  // (e.g. pulling latest project links)
}
